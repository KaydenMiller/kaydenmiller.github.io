@using KaydenMiller.Components
@attribute [Stories("Components/Skill")]

<Stories TComponent="Skill">
    <ArgType For="s => s.Name" Control="ControlType.Default" />
    <ArgType For="s => s.CompetenceLevel" Control="ControlType.Default" />
    
    <Story Name="Default">
        <Arguments>
            <Arg For="s => s.Name" Value='"C#"' />
            <Arg For="s => s.CompetenceLevel" Value="@(90)" />
        </Arguments>
        
        <Template>
            <Skill @attributes="context.Args!"></Skill>
        </Template>
    </Story>
</Stories>

@code {
    
}